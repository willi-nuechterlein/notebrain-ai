import { Box } from 'components/atoms/Box'
import { styled } from 'stitches.config'

const TileTextContainer = styled(Box, {
  position: 'relative',
  padding: '$5',
  width: '100%'
})

export default TileTextContainer
